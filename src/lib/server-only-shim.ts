/**
 * Minimal shim for the `server-only` package.
 * In real deployments Next.js provides this to guard server modules.
 * Here we simply export a no-op placeholder to satisfy bundling.
 */
export {};
